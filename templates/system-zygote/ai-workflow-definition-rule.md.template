# AI Workflow Definition Rule

## 适用场景

当新增、修改或验证 Workflow、Skill、Agent、Executor、Rule、Schema、Template 或 Eval 时，必须遵守本规则。

## 核心约束

- Workflow 必须有触发、输入、输出、节点、验收和失败处理。
- Agent 不能绕过 Workflow。
- 确定性操作优先进入 Executor。
- 关键交接必须有 Schema 或明确说明。
- 正式 Workflow 必须有验证或 Eval。
