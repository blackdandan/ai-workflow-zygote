# AI Workflow System Zygote Evolution Workflow

<!-- 中文注释：本 Workflow 用于修改、升级和验证 zygote 生成机制。 -->

## 目标

修改、升级和验证 `ai-workflow-system-zygote` 生成机制本身。

## 适用场景

- 修改 zygote 生成的新系统结构。
- 修改 zygote 模板或执行器。
- 修改默认元能力。
- 修改 zygote 成功标准或 Schema。

## 不适用场景

- 从零创建新系统，应使用 `ai-workflow-system-zygote`。
- 修复旧项目，应使用 `ai-workflow-system-reprogramming`。
- 新增普通资产，应使用 `ai-workflow-asset-evolution`。

## 执行步骤

1. 判断本次变更是否影响 zygote。
2. 对照 Anthropic 和 OpenAI 的 workflow、agent、harness、guardrails 和 orchestration 定义。
3. 检查四个元能力是否受影响。
4. 形成修改计划。
5. 执行修改。
6. 用 zygote 生成样例系统。
7. 验证样例系统独立性、四元能力、Entry、Router、Miss、Harness 和 Eval。

## 成功标准

- zygote 修改有计划、有验证。
- 新系统拥有四个元能力。
- 新系统没有母体品牌残留。
- 新系统包含 AI Workflow 定义、Harness、Agent 边界和 Eval。
