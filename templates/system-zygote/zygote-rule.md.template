# AI Workflow System Zygote Rule

<!-- 中文注释：本规则约束当前系统如何生成下一代独立 AI Workflow 工程。 -->

## 核心原则

### 1. 新工程必须独立

新工程不得依赖母体路径、仓库或品牌文案。

### 2. 新工程必须自带 zygote

新工程必须包含 `ai-workflow-system-zygote`，用于继续生成下一代系统。

### 3. 新工程必须自带资产演化能力

新工程必须包含 `ai-workflow-asset-evolution`，用于后续演化自身资产。

### 4. 脚本不交互

执行器只接收参数，不主动询问用户。
