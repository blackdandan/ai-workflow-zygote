# {{SYSTEM_NAME}} System Construction Rule

<!-- 中文注释：这是新系统的第一条构建规则，用于约束系统自身如何演化。 -->

## 适用场景

当任务涉及 `{{SYSTEM_NAME}}` 的目录设计、Workflow 建模、Skill 设计、Agent 设计、规则沉淀、模板更新或系统演化时，必须遵守本规则。

## 系统目标

{{SYSTEM_GOAL}}

## 核心原则

### 1. Workflow 优先

新增能力前，先判断它服务于哪个 Workflow。

### 2. 路由优先

正式 Workflow 必须包含自己的 `ROUTER.md`，并被主 Router 聚合。

### 3. 契约优先

关键输入输出必须有 Schema、模板或等价校验。

### 4. 中文说明

正式资产文件必须包含必要中文说明，帮助人类和 AI 理解用途。

### 5. 演化闭环

新增或修改资产后，必须验证并更新 `docs/workflows.md`。
